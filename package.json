{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "",
  "main": "portfolio_script.js",
  "scripts": {
    "build:toolbar": "esbuild toolbar-entry.ts --bundle --format=iife --target=es2017 --minify --outfile=toolbar-bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@21st-extension/toolbar": "^0.5.14"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
